// a position fixed creature attached to the cursor for placement
import React, { useState, useEffect, useCallback } from 'react'

const CreatureCursor = ({placement}) => {

//     const [mouseCoords, setMouseCoords] = useState({
//         x: 0,
//         y: 0
//     })
// // I need a cb memoized

//     const updateCoords = useCallback(() => {

//     }, [])

//     useEffect(() => {
//         if (!placement) { return }

//     }, [placement])

    return (
        <>

        </>
    )
}

export default CreatureCursor